first we trained an autoencoder
then transfer the weights from encoder path of autoencopder to encoder of unet 
the uner is a unet with residual blocks
this model replaces the residual blocks in the generator of srgan